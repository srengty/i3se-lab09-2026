# Initial Jenkins POLL SCM
Next step is to create a Jenkins job that will poll the SCM for changes and trigger a build when changes are detected.

# Wait for changes
POLL SCM is a feature in Jenkins that allows you to automatically check for changes in your source code repository at regular intervals. When changes are detected, Jenkins will trigger a build of your project.